# Expo client environment – copy to `.env` or `.env.local` and fill in values.
# Never commit real secrets (especially service role keys).

EXPO_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJ...
EXPO_PUBLIC_WHOP_CHECKOUT_URL=https://whop.com/<your-checkout-slug>
EXPO_PUBLIC_SUPPORT_EMAIL=support@fitkofer.com

# Optional integrations
EXPO_PUBLIC_SENTRY_DSN=
SENTRY_ORG=
SENTRY_PROJECT=
EXPO_PUBLIC_POSTHOG_KEY=
EXPO_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# Local-only tooling (keep in a separate, untracked `.env.local`)
# SUPABASE_SERVICE_URL=https://<project-ref>.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=service-role-secret
